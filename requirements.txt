# Django & Core
Django==5.1.4
asgiref==3.8.1
sqlparse==0.5.5

# Production Server
gunicorn==23.0.0

# Static Files
whitenoise==6.12.0

# Database
psycopg2-binary==2.9.9
dj-database-url==2.3.0

# Environment Variables
python-dotenv==1.2.2

# Django Apps (your existing)
django-allauth==65.18.0
django-cors-headers==4.9.0
django-extensions==4.1
django-filter==24.3
django-jazzmin==3.0.4
django-q2==1.10.0
django-storages==1.14.6
django-widget-tweaks==1.5.1
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
dj-database-url==2.3.0
# API & Services
requests==2.34.2
PyJWT==2.13.0

# M-Pesa & Payments
# (No additional packages — uses requests)

# File Processing
Pillow==10.2.0
openpyxl==3.1.5
fpdf2==2.8.7
xhtml2pdf==0.2.17

# Utilities
python-dateutil==2.9.0.post0
PyYAML==6.0.3
tzdata==2026.2

# Cloud Storage (R2/S3 compatible) — uncomment when ready
# boto3==1.43.17
# botocore==1.43.17

# Removed (not used in production):
# celery, amqp, billiard, kombu, vine, click-* (Celery deps)
# channels, channels_redis, msgpack, redis (WebSocket deps)
# africastalking (not integrated yet)
# openai (not integrated yet)