name: Django CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_DB: property_test
          POSTGRES_USER: gwiternz
          POSTGRES_PASSWORD: password123  # Simple password for CI
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12' # Recommended: 3.12 is more stable for CI than 3.13 currently

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tests
      env:
        DATABASE_URL: postgresql://gwiternz:password123@localhost:5432/property_test
        DJANGO_SETTINGS_MODULE: property_mgmt.settings
      run: |

